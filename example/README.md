# dockerc examples

This directory is similar in spirit to Rust's `cargo run --example …`: it contains runnable configurations you can use as a starting point.

## Sample config

- Config: `example/sample_config/config.toml`
- Run: `make example`

Notes:
- Requires network access (pulls the image).
- Produces `example/out/alpine`.
